"""模块五：日志可视化分析。

### 它解决什么

日志文件本身是**给人看的**，但它排列的方式不是：几万行等宽文本，
真正要看的东西（哪个接口在报错、哪个 SQL 在拖后腿、错误集中在哪几分钟）
散在几千行里，靠 Ctrl+F 是找不齐的。

所以这里做的事是：**把日志读成一份报告，再从报告下钻回原始行**。
报告回答"是什么情况"，下钻回答"具体是哪几行" —— 中间不给用户留
"那我自己去 grep 一下"这一步。

### 形态：`native`（同进程挂载）

- 只需要读文件 + 纯标准库解析，没有长任务、没有外部依赖
- 不起端口、不起进程 —— 少一个端口就少一处要防的地方
- 自带页面（`web/`），中台「模块」面板一点就能用

### ⛔ 与模块四（本地文件列表）的关键差别

模块四的约束里写着「**不读文件内容**」。本模块恰恰是第一个越过这条线的：

| | 模块四 filelist | 本模块 logviz |
|---|---|---|
| 碰文件内容 | ⛔ 明确不读 | ✅ 必须读（逐行流式扫描） |
| 界面上的动作 | 浏览、看大小 | 分析、看报告 |
| 安全阀 | 登记根目录 + 路径包含性校验 | **同一套**（`paths.py` 复刻了一份） |

「只读」在这里要拆成两半说，否则会自相矛盾：

- **对用户的日志文件：零写操作。** 不开写模式、不删、不移、不改名、
  不截断、不轮转。任何情况下都不回写一个字节。
- **对本模块自己：会写。** 分析摘要缓存进模块自己的 SQLite
  （`data/logviz.db`）—— 否则每次打开都重扫一个几百 MB 的文件，
  这个工具就没法用了。缓存的 key 是「路径 + 大小 + mtime」，
  文件一改缓存自动作废，不会拿旧报告骗人。

### 边界（不许放宽，也不要"优化"掉）

- 能读的范围限于用户**亲手登记**的日志根目录，路径做包含性校验
- 一次扫描有明确预算（字节数 / 行数 / 耗时），到顶如实标注「没读完」
- 不提供任何形式的命令执行，不联网，不上传
- 解析器只解析，不"猜"：认不出来的格式如实说认不出来，不硬套
"""

MODULE = ModuleSpec(
    id="logviz",
    name="日志可视化分析",
    description=(
        "把日志读成一份报告：时间分布、状态码/级别构成、Top URL/异常/SQL，"
        "每个指标都能下钻回原始行；支持 nginx access、Java 应用日志、MySQL 慢查询"
    ),
    version="1.0",
    order=30,
    kind="native",
    app_module="logviz_api",
    router_object="router",
    license="自有项目（本项目自研，非第三方）",
    note=(
        "对用户的日志文件全程只读（不开写模式、不删不移不改名）；"
        "可读范围限于用户登记的日志根目录，路径做包含性校验。"
        "分析摘要缓存在模块自己的 data/logviz.db 里，文件一变缓存自动作废。"
    ),
)
