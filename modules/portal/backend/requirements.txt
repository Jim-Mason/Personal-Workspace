# 开发工具箱 —— 后端依赖
# 安装：python -m venv .venv && .venv/Scripts/pip install -r requirements.txt

fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
sqlalchemy>=2.0,<3.0
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
PyJWT>=2.9,<3.0
httpx>=0.27,<1.0
websockets>=13,<16
python-multipart>=0.0.12
# 卡片配置的 xlsx 导入导出（CSV 用标准库 csv，不需要额外依赖）
openpyxl>=3.1,<4.0
